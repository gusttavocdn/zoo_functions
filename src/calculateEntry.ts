import { species } from '../data/zoo_data';

type Visitants = {
  child: number
  adult: number
  senior: number
};

type Entrant = {
  name: string
  age: number
};

const countEntrants = (entrants: Entrant[]): Visitants => {
  return null as any;
};

const calculateEntry = (entrants: Entrant[]): Visitants => {
  return null as any;
};

export { calculateEntry, countEntrants };
